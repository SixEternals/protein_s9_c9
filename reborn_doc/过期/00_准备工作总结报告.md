# CRISPR/Cas9 Off-Target Fusion 项目 — 准备工作总结报告

> 报告人：Kimi Code CLI（初始环境准备 Agent）  
> 时间：2026-05-23  
> 状态：**准备工作已完成，等待 Kimi 审核并进入 P0 开发阶段**

---

## 一、本次准备工作的定位

本次工作属于项目正式启动前的**基础设施与规范搭建阶段**。在正式编码 BL0 ~ BL6 之前，必须先确立：
1. 项目总纲（所有 Agent 的统一认知）
2. 版本管理规范（从子文件夹模块系统迁移到 Git 多版本系统）
3. 多 AI 协作约束（确保 Kimi / Codex / Claude 在同一套规则下工作）

**结论**：准备工作已就绪，可以进入 `reborn_doc/1. 大纲拟定.md` 中定义的 P0 阶段。

---

## 二、已完成的具体事项

### 2.1 项目总纲重写：`reborn_doc/1. 大纲拟定.md`

**原状态**：原文件仅 68 行，是一份非常粗略的项目概述，缺少技术细节、执行计划和版本管理规范。  
**新状态**：重写为 225 行的完整执行计划 v2，包含以下六大章节：

| 章节 | 核心内容 | 对后续开发的意义 |
|:---|:---|:---|
| 一、项目现状与目标 | 明确已完成 R9/DeepFocus + C9/ConMismatch9 两套系统；下一步目标是三视角融合（Region + 全局 Run + RNA-FM） | 防止 Agent 重复造轮子或偏离主线 |
| 二、核心概念定义 | 坐标约定（position 1 = PAM-distal）、术语规范（unobserved_candidate）、Tier 分层权重、**全局 Run 编码详解**（4 种状态 + Seed 梯度加权 A/B/C 三种方案） | 消除后续编码中的坐标歧义和术语混乱；Run 编码是 BL3/BL5 的核心输入 |
| 三、版本管理铁律 | **Git 多版本系统**：禁止复制文件夹做版本管理；禁止危险 git 操作；强制使用分支 + YAML config + 附注 tag | 彻底终结旧项目的子文件夹版本管理陋习，保证代码库干净可回溯 |
| 四、执行计划 | P0（阻塞项：数据集审计、RNA-FM 验证、坐标审计、BL0）→ P1（BL3-hard + Tier-aware Loss）→ P2（BL4 + BL5 系列融合）→ P3（BL6 + LOO 验证） | 给 Kimi 提供清晰的开发路线图和优先级判断依据 |
| 五、关键检查清单 | 6 项硬性检查，包括数据集字段、RNA-FM 规格、坐标方向、Run 编码范围、Git tag 规范 | 每个里程碑结束时的验收标准 |
| 六、补充材料索引 | CCLMoff 链接、RNA-FM 地址、师兄论文位置、历史 baseline 记录位置 | 快速定位参考资料，避免重复提问 |

### 2.2 多 AI 协作约束文件（Skill / Instructions）

为确保 Kimi、Codex、Claude 三个 Agent 在同一套规则下协作，创建了**三个平台对应的约束文件**，内容同源、语言适配：

| 文件路径 | 目标 Agent | 语言 | 核心约束维度 |
|:---|:---|:---:|:---|
| `AGENTS.md` | **Kimi** | 中文 | Git 铁律、坐标约定、术语规范、代码风格、文件操作规范 |
| `.codex` | **Codex CLI** | 英文 | 同上 |
| `.claude/CLAUDE.md` | **Claude Code** | 英文 | 同上 |

**三个文件共同锁定的 Git 红线**：
1. 🚫 **禁止复制文件夹做版本管理**（如 `baseline_v2/`、`model_new/`、`experiments_backup/`）
2. 🚫 **禁止危险 Git 命令**（`push --force`、`reset --hard`、`rebase`、`clean -f`）
3. 🚫 **禁止未经确认执行 `git commit` / `git push`**
4. ✅ **版本切换必须通过 Git 分支 + YAML config**
5. ✅ **每个 BL 完成后必须打附注 tag**（格式 `BL{n}-{sub}-v{x.y}`，附注消息必须含 AUROC/AUPRC/GPU/epoch）
6. ✅ **实验结果必须同步写入 `results/experiments.csv`**

### 2.3 旧项目结构的整理与映射

在准备过程中，对现有代码库进行了结构梳理，明确了新旧映射关系：

| 旧项目中的概念 | 新大纲中的对应 |
|:---|:---|
| `offtarget_fusion_project/baseline0_cclmoff/` | **废弃**。BL0 将用 `models/bl0_cclmoff.py` + `configs/bl0.yaml` 替代 |
| `artifacts/full_k562_conmismatch9/` 等子文件夹 | **废弃**。所有实验结果归入 `results/checkpoints/`（gitignore），版本由 Git tag 管理 |
| `encoders/c9_encoder.py`、`encoders/r9_encoder.py` | 保留并扩展。新增加 `encoders/run_encoder.py`、`encoders/rnafm_encoder.py` |
| `models/conmismatch9_torch.py`、`models/deepfocus_torch.py` | 保留作为历史参考。新 BL 系列使用 `models/bl3_hard_prior.py`、`models/bl5_series.py` 等独立文件 |
| `configs/c9_conmismatch9.yaml`、`configs/r9_deepfocus.yaml` | 保留现有生产配置。新增 `configs/bl0.yaml` ~ `configs/bl6.yaml` 系列 |

---

## 三、待 Kimi 审核的关键决策点

以下事项需要 Kimi 确认或拍板，之后方可正式进入 P0 编码：

### 3.1 项目结构变更（是否接受？）
新大纲建议将 `offtarget_fusion_project/` 下的子模块内容逐步迁移到根目录的统一结构（`models/`、`configs/`、`scripts/`）。  
**问题**：是保留 `offtarget_fusion_project/` 作为独立子项目，还是将其内容合并到根目录？

### 3.2 Git 仓库现状
当前根目录 `/data/zwf/code1/reborn_seed` 已是一个 Git 仓库（存在 `.git/`），但历史提交记录较杂乱，且包含大量 `artifacts/`、`runs/` 等实验子文件夹。  
**问题**：
- 是否保留现有 Git 历史？
- 是否需要清理 `artifacts/`、`runs/` 中的旧子文件夹版本（这些属于被禁止的"复制文件夹版本管理"产物）？
- 是否为当前状态打一个归档 tag（如 `legacy-v1.0`）后再开始 BL0 开发？

### 3.3 编码器实现优先级
新大纲中定义了三种编码器：
1. `region_encoder.py` — Region 视角（9-bit，已有基础在 `c9_encoder.py` 中）
2. `run_encoder.py` — 全局 Run 视角（9-bit，**全新实现**）
3. `rnafm_encoder.py` — RNA-FM 预训练视角（640d，**全新接入**）

**问题**：P0 的 BL0 只需要 RNA-FM（复现 CCLMoff）。Region 和 Run 编码器是否在 P0 就提前搭建骨架，还是推迟到 P1（BL3-hard）再实现？

### 3.4 数据集来源
大纲中提到下载 CCLMoff 数据集（GitHub releases）并检查 `detection_method`、`source_study` 字段。  
**问题**：CCLMoff 数据集是否已经存在于服务器上？如果存在，路径在哪里？是否需要先从 GitHub 重新下载？

---

## 四、下一步建议动作（P0 启动清单）

若 Kimi 审核通过，建议按以下顺序启动 P0：

1. **确认数据集位置或下载 CCLMoff 数据集**
2. **运行 `scripts/audit_schema.py`** — 检查字段并生成 `data/tier_labels.csv`
3. **运行 `scripts/verify_rnafm.py`** — 安装 `rna-fm` 并验证 12 层/640 维/~99M 参数
4. **运行 `scripts/audit_coordinates.py`** — 对照旧代码确认 position 1 = PAM-distal
5. **实现 `models/bl0_cclmoff.py` + `configs/bl0.yaml`** — 复现 CCLMoff baseline
6. **训练并验证 BL0，记录到 `results/experiments.csv`，打 tag `BL0-v1.0`**

---

## 五、文件清单（本次准备阶段产出）

| 文件 | 说明 | 大小 |
|:---|:---|:---:|
| `reborn_doc/1. 大纲拟定.md` | 项目总纲与执行计划 v2 | 10,567 B |
| `reborn_doc/00_准备工作总结报告.md` | 本报告 | — |
| `AGENTS.md` | Kimi 约束文件 | 4,039 B |
| `.codex` | Codex CLI 约束文件 | 3,605 B |
| `.claude/CLAUDE.md` | Claude Code 约束文件 | 3,606 B |

---

## 六、附注

- 所有文件均已落地到 `/data/zwf/code1/reborn_seed/` 目录，未执行任何 `git commit` 或 `git push`（遵守 Git 铁律）。
- 未修改任何现有 `.py` 代码文件（仅阅读了 `README.md`、现有大纲、`5_23newOutline.md` 等进行信息整合）。
- 未删除任何 `data/`、`artifacts/`、`runs/` 下的用户数据（遵守文件操作规范）。
