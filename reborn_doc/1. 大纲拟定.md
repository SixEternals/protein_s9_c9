# CRISPR/Cas9 Off-Target Fusion 项目执行计划 v2

> 本文档是项目总纲。所有 AI 助手（Kimi / Codex / Claude）在执行任务前必须先阅读本文档，并严格遵守其中的 Git 管理铁律与坐标约定。
>
> ⚠️ **时效性说明**：本文档中的"执行计划"章节（P0-P3）反映的是早期规划，部分任务状态已过时。最新项目优先级和进行中的任务请务必查阅 **`reborn_doc/todo.md`**。实验结论和消融结果请以 **`reborn_doc/38_BL5_v4_PAM_组件消融总报告.md`** 和 **`reborn_doc/39_BL5_v4_PAM_泛化证据总报告.md`** 为准。
>
> 🚫 **`reborn_doc/过期/` 路径下的所有文件均不可作为当前事实依据引用。** 该目录仅作为重整历史文档时的参照存档。

---

## 一、项目现状与目标

### 已完成的里程碑

| 阶段 | 核心内容 | 关键结果 | 文档 |
|:---|:---|:---|:---|
| **P0** | 旧模型加权平均（R9+C9）+ 数据集审计 + RNA-FM 验证 | AUPRC=0.851 (GUIDE-seq)；CSV 639万行/11字段 | `reborn_doc/P0.md` |
| **BL0** | CCLMoff 复现基线：frozen vs fine-tune RNA-FM + MLP | frozen AUPRC=0.073；finetune AUPRC=0.522 (旧split) / 0.296 (formal split) | `reborn_doc/BL0.md` |
| **BL3-hard** | Region + Run 先验验证（无 RNA-FM）| Run-only AUPRC=0.609 > Region+Run=0.555 (GUIDE-seq) | `reborn_doc/BL3.md` |
| **BL4** | Frozen RNA-FM + Run CNN | BL4-finetune AUPRC=0.490 (CCLMoff)；BL4-full 未实现 | `reborn_doc/BL4.md` |
| **BL5-v4-PAM** | 当前主模型：RNA-FM + LearnableRun + PAM (simple concat) | **AUPRC=0.5313** (formal split, 954k test, 72 unseen sgRNA) | `reborn_doc/BL5.md` |
| **BL5 消融矩阵** | 9 个配置完整组件消融 | RNA-FM+Run 是主驱动力；PAM 是条件性增量 (+0.0289) | `38_BL5_v4_PAM_组件消融总报告.md` |
| **BL5 泛化** | sgRNA-safe + AGG PAM holdout + external audit | unseen sgRNA ✅；AGG holdout 发现 PAM 负迁移；cross-dataset 不可行 | `39_BL5_v4_PAM_泛化证据总报告.md` |
| **BL6-1** | PAM-Gated Fusion on BL5-v4-PAM backbone | AUPRC=0.5399 (+0.0086, single-run, bootstrap CI 不跨 0) | `40_BL6_1_PAM_Gated_Fusion_证据边界与待补项.md` |

### 当前主线模型

**BL5-v4-PAM** 是项目当前主结果：
- `models/bl5_dynamic_fusion.py`
- `encoders/learnable_run_encoder.py` + `encoders/pam_encoder.py`
- `configs/bl5_v4_pam.yaml`
- 评估基于 `formal_split_bl5_seed42.json`（test: 954,326 样本，3,057 observed_positive，72 个 unseen sgRNA_type）

### 下一步优先事项（来源：`reborn_doc/todo.md`）

| 优先级 | 任务 | 状态 |
|:---|:---|:---|
| 🔴 P0 | BL6-1 gate audit（eval-only export gate weights）| 待做 |
| 🔴 P0 | 修正 `experiments.csv` / `report.md` 中 "Cross-Attn + Softmax Gate" 模板错误 | 待做 |
| 🟡 P1 | BL6-1 multi-seed repeat（seed 43, 44）| 待做 |
| 🟡 P1 | TGG strict PAM-holdout 审核 | 🔄 Kimi 进行中 |
| 🟡 P1 | BL5-v4-PAM multi-seed（补第 3 个 seed）| 待做 |
| 🟢 P2 | BL4-full formal split（路线完整性）| 待做 |
| 🟢 P2 | kNN baseline、in-silico perturbation | 待做 |
| ⚪ P3 | Tier-aware Loss Weighting、BL3b seed regression | 低优先级 |

> 详细优先级、验收口径和解释边界见 **`reborn_doc/todo.md`**。

---

## 二、核心概念定义

### 坐标约定（硬性约束，不可违背）
- **position 1–20**: protospacer / guide-target aligned region
- **position 1**: PAM-distal end（最远端）
- **position 20**: PAM-proximal end（紧挨 PAM）
- **positions 21–23**: PAM region（NGG）
- **hard seed**: positions 16–20（工程约定，非生物学真理）

> ⚠️ 风险：如果坐标方向反了，Region 编码的 seed/ordinary 标记全错。任何涉及位置索引的代码修改前，必须对照此约定。

### 术语规范
- 不用 `"negative"`，用 `"unobserved_candidate"`
- `label=0` ≠ 安全位点，只是没被实验检测到
- `"positive"` 可用（表示实验检测到切割）

### 检测方法 Tier 分层（用于 Loss Weighting）
| Tier | 检测方法 | 权重 | 说明 |
|:---:|:---|:---:|:---|
| Tier 1 | DISCOVER-seq, DISCOVER-seq+, GUIDE-seq, HTGTS | 1.0 | 直接检测体内真实切割，假阳性率低 |
| Tier 2 | IDLV, BLESS, BLISS, PEM-seq, VIVO | 0.7 | 间接标记，可能漏检，中等可信度 |
| Tier 3 | CIRCLE-seq, CHANGE-seq, Digenome-seq, SITE-seq | 0.4 | 体外条件≠体内，CIRCLE-seq 假阳性率~88% |

### CCLMoff 数据集真实审计结论（必须在后续文档中继承）

已下载并校验 Figshare v2 文件 `data/cclmoff/09212024_CCLMoff_dataset.csv`：

- 文件大小：`714,692,329` bytes，MD5 `2a9be5c69a89c8eee3fdef0c03efae3a`，与 Figshare API 元数据一致
- 总行数：`6,393,373` 条样本，不是早期记录中的约 950 万
- 字段数：实际有 11 个字段，不是早期记录中的 5 个字段
- 字段：`sgRNA_seq`, `off_seq`, `read`, `sgRNA_type`, `label`, `chr`, `Location`, `Direction`, `Length`, `Method`, `id`
- 标签：`observed_positive=39,319`，`unobserved_candidate=6,354,054`，无非二值标签
- `Method` 字段存在，但 `3,282,935` 行为空；`Length` 字段同样有 `3,282,935` 行为空
- `sgRNA_type_unique=282`，`sgRNA_seq_unique=2,991`

标注语义必须保持：`label=1` 表示 `observed_positive`；`label=0` 表示 `unobserved_candidate`，不能解释为实验验证安全。`read` 字段与标签一致性较好：`observed_positive` 中 38,912 行 read>0、407 行 read 缺失；`unobserved_candidate` 中无 read>0。

Tier-aware 训练不能简单假设全量样本都有检测方法。当前策略是：对 `Method` 非空且已确认方法含义的样本可做 Tier 映射；`Method` 为空、`SURRO-seq`、`Extru-seq` 等未纳入 Tier 表的方法先标为 `unknown`，等待外部来源或论文补充材料确认。`sgRNA_type` 不是基因组位置信息，也不是检测方法字段。

额外注意：CSV 中序列长度混有 22/23/24nt，并包含 `-`。旧 R9/C9 模块的 P0 加权平均只使用本地 23nt NPZ 数据，不直接使用该混合 CSV。

### CCLMoff 权重审计结论

已下载 Figshare 文件 `data/cclmoff/CCLMoff_V1.ckpt`，MD5 `80b311715969524a3972ab75928f3969`，与 Figshare API 元数据一致。但该 checkpoint 的 `state_dict` 前缀为 `encoder`/`decoder`，缺少公开 `CCLMoff/my_model.py` 中的 `rna_model`、`dense1`、`dense2` 权重键。因此它**不能直接当作公开 CCLMoff RNA-FM + MLP 推理权重使用**，除非后续确认官方仓库另有匹配的 inference 代码或权重文件。RNA-FM 本体权重已单独验证为满血版，见 `results/audits/rnafm_verification_report.md`。

### 全局 Run 编码（★核心创新点★）

**Run** = 连续错配段。在 sgRNA（20nt）与 DNA 靶标比对时，看**连续有多少个位置错配**。

**4 种 Run 状态（2-bit 编码，位于 9-bit 向量的 b8-b9）**：
| 编码 | 名称 | 含义 |
|:---:|:---|:---|
| `00` | match | 碱基配对成功 |
| `01` | isolated | 孤立错配（左右都是 match） |
| `10` | run2 | 连续 2 个错配 |
| `11` | run3+ | 连续 3 个及以上错配 |

**关键原则：全局编码，不是局部编码**
- ❌ 局部 Run（错误）：只在 seed 区（16-20）算 Run 状态
- ✅ 全局 Run（正确）：**全部 20 个位置**都算 Run 状态，再结合 seed 梯度加权区分重要性

**Seed 梯度加权（在全局 Run 编码之后应用）**
- 方案 A - Hard 加权：positions 1-15 权重=1.0，positions 16-20 权重=2.0
- 方案 B - Soft 加权（推荐 baseline）：`weight(pos) = exp(-distance_to_PAM / tau)`，`tau=4` 或 `5`
- 方案 C - Learnable 加权：20 个位置各有一个可学习参数，初始化用 Soft 曲线

---

## 三、版本管理铁律（Git 多版本系统）

### 【绝对禁止】
1. **禁止复制文件夹做版本管理**。过去用 `offtarget_fusion_project/baseline0_cclmoff/`、`artifacts/full_k562_conmismatch9/` 等子文件夹保存不同版本的做法必须彻底废弃。
2. **禁止** `git push --force`、`git reset --hard`、`git clean -f`、`git rebase`（除非用户明确要求并确认）。
3. **禁止**在未经用户确认的情况下执行 `git commit`、`git push`。

### 【强制要求】
1. **所有版本通过 Git 分支 + config 文件切换**。同一套代码库，不同 baseline 用不同 YAML config 和不同模型入口文件。
2. **每个 BL 版本完成后必须打 tag**，格式：`BL{n}-{sub}-v{x.y}`，附注消息必须包含关键指标（AUROC、AUPRC、GPU 内存、epoch 等）。
3. **experiments.csv** 必须同步更新，作为人类可读的实验记录。

### Git 工作流程
```bash
# 1. 开发新 BL 版本时创建功能分支
git checkout -b feat/bl5-series

# 2. 开发与测试
# ... 编码、运行、验证 ...

# 3. 合并回 main 并打 tag（完成后必须执行）
git checkout main
git merge feat/bl5-series --no-ff -m "feat: BL5 series fusion implementations"
git tag -a BL5-3-v1.0 -m "BL5-3 Cross-Attn+Gated | AUROC=x.xx | AUPRC=x.xx | GPU=48GB"

# 4. 查看版本历史
git log --oneline --decorate --tags --graph
```

### 项目结构（面向 Git 多版本）

> 以下反映**实际代码库结构**，不同 Baseline 通过独立模型文件 + YAML config 切换，不复制目录。

```
reborn_seed/
├── .git/                          # 唯一版本管理源
├── AGENTS.md                      # AI 协作约束（本文档）
├── data/                          # 数据集目录 (gitignore)
│   ├── cclmoff/                   # CCLMoff Figshare v2 CSV
│   └── rnafm/                     # RNA-FM 预训练权重
├── datasets/
│   └── cclmoff_dataset.py         # CCLMoff 数据集加载器
├── encoders/                      # 编码器（独立目录，非 models/ 子目录）
│   ├── c9_encoder.py              # C9 / ConMismatch9 编码
│   ├── learnable_run_encoder.py   # LearnableRunEncoder (BL5-v4)
│   ├── pam_encoder.py             # PAM Encoder (positions 21-23)
│   ├── r9_encoder.py              # R9 / DeepFocus 编码
│   └── run_encoder.py             # 全局 Run 编码 + Seed 梯度
├── models/                        # 模型定义
│   ├── base.py
│   ├── bl0_cclmoff.py             # BL0: RNA-FM + MLP head
│   ├── bl3_hard_prior.py          # BL3: Region + Run 先验 (无 RNA-FM)
│   ├── bl3_5_fusion.py            # BL3.5: Region + Run + Cross-Attn
│   ├── bl3b_seed_regression.py    # BL3b: Seed regression
│   ├── bl4_rnafm_fusion.py        # BL4: Frozen RNA-FM + Run
│   ├── bl5_dynamic_fusion.py      # BL5-v4-PAM / BL6-1 主模型
│   └── conmismatch9_torch.py      # 历史 C9 模型参考
├── configs/                       # 各版本 YAML 配置
│   ├── bl0*.yaml                  # BL0 系列
│   ├── bl3*.yaml                  # BL3 系列
│   ├── bl4*.yaml                  # BL4 系列
│   ├── bl5*.yaml                  # BL5 系列（含 v3/v4/PAM/NoPAM/shuffle）
│   └── bl6*.yaml                  # BL6 系列
├── scripts/                       # 训练、审计、评估脚本
│   ├── train_bl0a.py              # BL0 训练入口
│   ├── train_bl3.py               # BL3 训练入口
│   ├── train_bl4.py               # BL4 训练入口
│   ├── train_bl5.py               # BL5 训练入口
│   ├── train_region_only_gpu.py   # Region-only GPU 脚本
│   ├── audit_*.py                 # 各类审计脚本
│   ├── evaluate_*.py              # 评估脚本
│   └── smoke_test_*.py            # Smoke test 脚本
├── utils/
│   ├── config.py                  # 配置解析
│   ├── guardrails.py              # 运行时约束检查
│   ├── io.py                      # IO 工具
│   ├── metrics.py                 # 指标计算
│   ├── rnafm.py                   # RNA-FM 安全加载 + tokenizer
│   ├── sequence.py                # 序列处理
│   └── training.py                # 训练辅助函数
├── run/                           # Bash 启动脚本
│   └── run_*.sh
├── tests/                         # 单元测试
├── results/                       # 实验结果 (gitignore)
│   ├── experiments.csv            # 实验记录表
│   └── */                         # 各版本结果子目录
├── reborn_doc/                    # 项目文档
│   ├── todo.md                    # ⚠️ 最新优先级快照
│   ├── 1. 大纲拟定.md             # 项目总纲（本文档）
│   ├── P0.md, BL0.md, BL3.md      # 各版本历史归档（已整合）
│   ├── BL4.md, BL5.md, 其他.md    # 各版本历史归档（已整合）
│   ├── 26_~40_系列.md             # 单实验/审计/总报告
│   ├── 38_组件消融总报告.md       # 最新消融总结
│   ├── 39_泛化证据总报告.md       # 最新泛化总结
│   └── 过期/                      # 原始历史文件存档（⚠️ 不可引用）
└── requirements.txt
```

### experiments.csv 格式
```csv
version,date,commit_hash,auroc,auprc,auprc_t1,auprc_t2,auprc_t3,train_time,gpu_mem,epochs,best_epoch,config_path,notes
BL0,2025-05-23,abc123,0.985,0.520,0.55,0.48,0.30,4h,48GB,50,42,configs/bl0.yaml,CCLMoff reproduction
BL3-hard,2025-05-30,def456,?,?,?,?,?,?,?,?,?,configs/bl3_hard.yaml,Soft gradient tau=4
```

---

## 四、历史执行计划与当前状态

> 本章节记录项目早期规划（P0-P3）及实际完成情况。当前优先事项见**一、项目现状与目标 → 下一步优先事项**，或查阅 **`reborn_doc/todo.md`**。

### P0 - 基础设施与基线（已完成）

| 任务 | 内容 | 实际状态 |
|:---|:---|:---:|
| 1. 数据集 Schema/Label 审计 | 下载 CCLMoff CSV，校验 MD5、字段、标签、read 一致性、Method 缺失情况 | ✅ 已完成 |
| 2. RNA-FM 权重验证 | `rna-fm==0.2.2`，验证 12 层/640 维/99.5M 参数，跑通前向传播 | ✅ 已完成 |
| 3. 坐标约定审计 | 对照旧代码确认 position 1 = PAM-distal；Run 覆盖 positions 1-20 | ✅ 已完成 |
| 4. P0/BL0 加权平均 | R9/DeepFocus + C9/ConMismatch9 验证集调权，不训练新网络 | ✅ 已完成 |
| 5. CCLMoff 严格复现 | RNA-FM + MLP head；Figshare ckpt 与公开代码不匹配，改用自训练 | ✅ BL0a/BL0b 已完成 |

### P1 - BL3-hard 先验验证（已完成）

| 任务 | 内容 | 实际状态 |
|:---|:---|:---:|
| BL3-hard 实现 | Region + Run + Seed 梯度（Hard/Soft-tau4/Learnable 三组） | ✅ 已完成（GUIDE-seq）|
| Tier-aware Loss Weighting | Tier 加权 loss + 分层评估 | ⚠️ 未实现，低优先级 |

### P2 - BL4/BL5 融合探索（已完成主线）

| 任务 | 内容 | 实际状态 |
|:---|:---|:---:|
| BL4 - Frozen RNA-FM + Prior | `concat(RNA-FM, Run)`，frozen/finetune 对照 | ✅ BL4-Run-only/frozen/finetune 已完成；BL4-full 未实现 |
| BL5 系列 - 三视角融合 | 原规划：BL5-0/1/2/3（Cross-Attn/Gated） | ⚠️ 旧路线（Cross-Attn/Gated）AUPRC 0.44-0.52，未超越 simple concat |
| **BL5-v4-PAM** | **实际主模型**：RNA-FM CLS + LearnableRun + PAM Encoder + simple concat | ✅ **AUPRC=0.5313，当前最强 baseline** |
| BL5 消融矩阵 | RNA-FM / LearnableRun / PAM 全组合 + shuffle control | ✅ 9 个配置已完成 |

### P3 - BL6 优化与泛化（部分完成）

| 任务 | 内容 | 实际状态 |
|:---|:---|:---:|
| BL6-1 PAM-Gated Fusion | BL5-v4-PAM + sample-wise softmax gate | ✅ Single-run 完成，AUPRC=0.5399 |
| BL6-1 证据补全 | gate audit、multi-seed、bootstrap CI、top-k | ⚠️ bootstrap CI ✅；gate audit / multi-seed 待做 |
| PAM-holdout 泛化 | AGG/TGG/GAG strict holdout | 🔄 AGG ✅；TGG 进行中；GAG 待做 |
| Leave-One-sgRNA-Out | 按 sgRNA 划分 fold | ⚠️ 暂不建议全量；formal split 已含 72 unseen sgRNA |
| Cross-dataset | 外部数据集验证 | ❌ 不可行；feasibility audit 结论：无 ready 外部 benchmark |

---

## 五、关键检查清单

开始前必须确认：
- [x] CCLMoff CSV 已下载并校验；真实字段为 11 个，`Method` 存在但约 328 万行为空
- [x] RNA-FM 是 12 层 / 640 维满血版，参数量 99,521,546
- [x] position 1 = PAM-distal（与旧代码一致）
- [x] Run 编码覆盖全部 20 位置（不是只 seed 区）
- [x] PAM 提取使用 `off_seq[20:23]`（positions 21-23），禁止 `off_seq[-3:]`
- [ ] 每个 BL 版本完成 = 代码 commit + git tag + `experiments.csv` 记录
- [x] **没有通过复制文件夹来保存不同版本**

### AI 文档引用纪律

> ⚠️ **新增约束**：`reborn_doc/过期/` 目录下的所有原始历史文件**不可作为当前事实依据引用**。如需了解某版本历史，应查阅根目录下对应的整合文档（`P0.md`、`BL0.md`、`BL3.md`、`BL4.md`、`BL5.md`、`其他.md`），或直接查阅最新总结文档（`38_组件消融总报告.md`、`39_泛化证据总报告.md`、`40_BL6-1_证据边界.md`）。
>
> 最新项目状态始终以 **`reborn_doc/todo.md`** 为准。

---

## 六、补充材料

- CCLMoff 论文 + 代码 + 模型：见 `reborn_doc/CCLMoff_links.md`
- RNA-FM 官方 GitHub：https://github.com/ml4bio/RNA-FM
- 师兄论文与历史文档：见 `doc/unpacked/` 与 `doc/` 目录
- 历史 baseline 记录：见 `doc/05_多数据集full_baseline记录.md` 等
